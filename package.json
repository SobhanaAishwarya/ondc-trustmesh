{
  "name": "ondc-blockchain-ai-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for the Blockchain-AI Enhanced ONDC trust scoring and dispute-resolution system",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
